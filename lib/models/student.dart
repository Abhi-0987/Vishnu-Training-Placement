class Student {
  final String email;
  bool isSelected;
  final bool isPresent;

  //student model
  Student({
    required this.email,
    this.isSelected = false,
    this.isPresent = false,
  });
}
